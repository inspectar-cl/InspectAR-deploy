version: '3.8'

services:
  apigateway:
    build: ./apigateway
    container_name: apigateway
    ports:
      - "3500:3500"
    environment:
      - NODE_ENV=development
    volumes:
      - ./apigateway:/usr/src/app
      - /usr/src/app/node_modules
  iot-service:
    build:
      context: ./microservicios/ParserService
      dockerfile: Dockerfile
    container_name: iot-service
    ports:
      - "8090:8090"
    depends_on:
      - mongu
      - influxdb
  emqx:
    image: emqx/emqx:latest
    container_name: emqx
    ports:
      - "1883:1883"    # MQTT
      - "8083:8083"    # WebSocket MQTT
      - "8081:8081"    # Dashboard HTTP API
      - "18083:18083"  # EMQX Dashboard
    environment:
      - EMQX_NAME=emqx
      - EMQX_ALLOW_ANONYMOUS=true
      - EMQX_LOADED_PLUGINS=emqx_management,emqx_recon,emqx_retainer,emqx_dashboard,emqx_rule_engine,emqx_bridge_rabbitmq
    depends_on:
      - rabbitmq

  rabbitmq:
    image: rabbitmq:3-management
    container_name: rabbitmq
    ports:
      - "5672:5672"    # RabbitMQ protocolo AMQP
      - "15672:15672"  # RabbitMQ Dashboard
    environment:
      - RABBITMQ_DEFAULT_USER=admin
      - RABBITMQ_DEFAULT_PASS=admin
    
  mongu:
    image: mongo:latest
    container_name: mongu
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

  influxdb:
    image: influxdb:2.7
    container_name: influxdb
    ports:
      - "8086:8086"
    volumes:
      - influxdb_data:/var/lib/influxdb2
    environment:
      - INFLUXDB_ADMIN_USER=admin
      - INFLUXDB_ADMIN_PASSWORD=admin123
      - INFLUXDB_BUCKET=sensores
      - INFLUXDB_ORG=my-org
      - INFLUXDB_TOKEN=my-super-token
      - DOCKER_INFLUXDB_INIT_MODE=setup
      - DOCKER_INFLUXDB_INIT_USERNAME=admin
      - DOCKER_INFLUXDB_INIT_PASSWORD=admin123
      - DOCKER_INFLUXDB_INIT_ORG=my-org
      - DOCKER_INFLUXDB_INIT_BUCKET=sensores
      - DOCKER_INFLUXDB_INIT_ADMIN_TOKEN=my-super-token

volumes:
  mongo_data:
  influxdb_data:
